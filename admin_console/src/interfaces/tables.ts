export type User = {
  no: string;
  name: string;
  date: string;
  status: string;
};

export type Pool = {
  no: string;
  name: string;
  date: string;
  status: string;
  members: number;
};

export type Transaction = {
  no: string;
  name: string;
  date: string;
  status: string;
  members: string;
};
